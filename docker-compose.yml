services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - clips_data:/data
    env_file:
      - ./backend/.env
    restart: unless-stopped

volumes:
  clips_data:
